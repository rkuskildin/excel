# Деплой-переменные веб-морды (docker compose подставляет их). Скопируй в deploy/.env.
# Секреты модели (ключ) — в КОРНЕВОМ .env (../.env), не здесь.

WEB_USER=admin
WEB_PASSWORD=change-me-please
WEB_PORT=8600
WEB_PROFILE=skill
